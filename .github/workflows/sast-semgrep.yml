name: Reusable SAST - Semgrep

on:
  workflow_call:
    inputs:
      scan-ref:
        description: 'Directory or ref to scan (default: .)'
        required: false
        type: string
        default: '.'
      config:
        description: 'Semgrep config (default: auto)'
        required: false
        type: string
        default: 'auto'
      fail-on-findings:
        description: 'Fail pipeline on findings (default: true)'
        required: false
        type: boolean
        default: true

jobs:
  semgrep:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Semgrep
        run: pip install semgrep

      - name: Run Semgrep SAST
        run: |
          # Convert comma-separated configs to multiple --config flags
          CONFIGS="${{ inputs.config }}"
          CONFIG_FLAGS=""
          IFS=',' read -ra CONFIG_ARRAY <<< "$CONFIGS"
          for cfg in "${CONFIG_ARRAY[@]}"; do
            # Trim whitespace
            cfg=$(echo "$cfg" | xargs)
            CONFIG_FLAGS="$CONFIG_FLAGS --config $cfg"
          done
          semgrep scan $CONFIG_FLAGS --sarif --output semgrep.sarif .
        continue-on-error: ${{ !inputs.fail-on-findings }}

      - name: Upload Semgrep SARIF to GitHub Security
        if: always()
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: semgrep.sarif
          category: semgrep
        continue-on-error: true

      - name: Upload Semgrep results as artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: semgrep-results
          path: semgrep.sarif
          retention-days: 30
